
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to all documents for everyone, including unauthenticated users.
    // This is a temporary measure to debug the permission issues.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
